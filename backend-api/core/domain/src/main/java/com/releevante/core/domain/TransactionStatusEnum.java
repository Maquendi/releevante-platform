package com.releevante.core.domain;

public enum TransactionStatusEnum {
  RETURNED_ON_TIME,
  RETURNED_BEFORE_TIME,
  PARTIAL_RETURN,
  RETURNED_OVERDUE,
  CURRENT,
  OVERDUE,
  PENDING,
  CHECKING_OUT
}
